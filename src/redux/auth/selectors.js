export const selectAuthUser = (s) => s.auth.user;
export const selectAuthLoading = (s) => s.auth.loading;
export const selectAuthPending = (s) => s.auth.pending;
export const selectAuthError = (s) => s.auth.error;
